{
  "name": "@my-app/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote --port 8787",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cline/d1": "^1.0.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.0",
    "wrangler": "^3.0.0"
  }
}
